# Cross-Validation-Bootstrap-Resampling-Python
Implements cross-validation (LOOCV, K-Fold, Validation Set) and Bootstrap resampling techniques in Python to estimate model test error, perform model selection, and quantify statistical uncertainty across credit default, stock market, and housing datasets.
